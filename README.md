# Controlla la tua bolletta

Sito costruito automaticamente dai sorgenti (privati). La bolletta viene letta dentro il browser e non viene inviata a nessun server: il sito e' fatto di soli file statici.
